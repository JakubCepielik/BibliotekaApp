@model IEnumerable<Biblioteka.Models.Ksiazki>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa )
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.KsiazkiId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.KsiazkiId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.KsiazkiId })
            </td>
        </tr>
}
    </tbody>
</table>
